export enum DATE_IDS {
  seconds = 's',
  minutes = 'min',
  hours = 'h',
  days = 'd',
  weeks = 'w',
  years = 'y',
}

export enum TIME_CONSTANTS {
  secondsInMinute = 60,
  minutesInHour = 60,
  hoursInDay = 24,
  daysInWeek = 7,
  weeksInYear = 52,
  daysInYear = 365,
}
