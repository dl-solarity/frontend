export enum PERIOD_IDS {
  seconds = 's',
  minutes = 'min',
  hours = 'h',
  days = 'd',
  weeks = 'w',
  months = 'mon',
  years = 'y',
}

export enum PERIOD_CONSTANTS {
  secondsInHour = 3600,
  hoursInDay = 24,
  daysInWeek = 7,
  daysInMonth = 30,
  daysInYear = 360,
}
